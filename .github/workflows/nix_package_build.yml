name: ros_package_build

on:
  workflow_call:
    secrets:
      PUSH_TOKEN:
        required: true
      CACHIX:
        required: true

  workflow_dispatch:

jobs:

  build_amd64:
    uses: ctu-mrs/ci_scripts/.github/workflows/nix_package_build_amd64.yml@nix
    secrets:
      PUSH_TOKEN: ${{ secrets.PUSH_TOKEN }}
      CACHIX: ${{ secrets.CACHIX }}
