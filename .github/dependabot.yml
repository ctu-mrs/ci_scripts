version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "ros2"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "08:00"
      timezone: "Europe/Prague"

    # Bundles all action updates into ONE single PR
    groups:
      action-updates:
        patterns:
          - "*"
  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "master"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "08:00"
      timezone: "Europe/Prague"

    # Bundles all action updates into ONE single PR
    groups:
      action-updates:
        patterns:
          - "*"
